echo "🧪 Running tests..."
pnpm test --lastCommit
TEST_STATUS=$?

if [ $TEST_STATUS -ne 0 ]; then
  echo "❌ Push blocked: tests failed"
  exit 1
fi

echo "✅ Tests passed. Proceeding with push..."
